<h1>Editing story</h1>

<% form_for [:admin, @story] do |f| %>
  <%= f.error_messages %>

  <%= render :partial => "form", :locals => {:f => f} %>
  
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @story %> |
<%= link_to 'Back', admin_stories_path %>